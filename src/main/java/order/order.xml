<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="order">
 <select id="count1" resultType="int">
  select count(*) from p_order
  where delivery = '상품 준비중' and userid = #{userid}
 </select>
  <select id="count2" resultType="int">
  select count(*) from p_order
  where delivery = '배송중' and userid = #{userid}
 </select>
  <select id="count3" resultType="int">
  select count(*) from p_order
  where delivery = '배송완료' and userid = #{userid}
 </select>
 
 <select id="orderlist" resultType="order.dto.OrderDTO">
  select * from p_order
  where userid= #{userid} and delivery in('상품 준비중','배송중','배송완료')
 </select>
 
 <select id="cancellist" resultType="order.dto.OrderDTO">
  select * from p_order
  where userid= #{userid} and delivery = '주문취소'
 </select>
 
 <select id="returnlist" resultType="order.dto.OrderDTO">
  select * from p_order
  where userid= #{userid} and delivery in('반품신청', '반품완료')
 </select>
 
 <insert id="addcartItem">
  insert into p_cart(idx, picture, iname, sale_price, point, volume)
  (select idx, picture, iname, sale_price, point, volume
  from p_items
  where idx = #{idx})
 </insert>
 
 <update id="addcartid">
 update p_cart 
 set cartid = (select nvl( max(cartid)+1, 1) from p_cart), num=#{num}
 where idx=#{idx}
 </update>
 
 <select id="cartView" resultType="order.dto.CartDTO">
  select * from p_cart
  order by cartid
 </select>
 
 <update id="numUp">
  update p_cart
  set num = num+1
  where idx = #{idx}
 </update>
 
  <update id="numDown">
  update p_cart
  set num = num-1
  where idx = #{idx}
 </update>
 
 <delete id="deleteCart">
   delete from p_cart
   where cartid=#{cartid}
 </delete>
 
 
 </mapper>